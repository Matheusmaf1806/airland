{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    // Serve todas as páginas através de "server.js"
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}
